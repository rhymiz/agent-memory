{
  "name": "agent-memory",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/client/memory-client.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run format:check && bun run typecheck && bun test",
    "retrieval:evaluate": "bun run scripts/evaluate-retrieval.ts",
    "baseline:doctor": "uv tool run --from agent-baseline==0.4.0 agent-baseline doctor . --agent codex",
    "baseline:check": "uv tool run --from agent-baseline==0.4.0 agent-baseline check .",
    "baseline:record": "uv tool run --from agent-baseline==0.4.0 agent-baseline record .",
    "baseline:verify": "uv tool run --from agent-baseline==0.4.0 agent-baseline verify .",
    "format": "prettier --write src tests examples scripts .github AGENTS.md README.md docs skills .agent-baseline.json package.json tsconfig.json",
    "format:check": "prettier --check src tests examples scripts .github AGENTS.md README.md docs skills .agent-baseline.json package.json tsconfig.json",
    "demo": "bun run examples/two-agents.ts",
    "build": "bun run scripts/build.ts",
    "model:download": "bun run scripts/download-model.ts",
    "verify:binary": "sh scripts/verify-binary.sh"
  },
  "engines": {
    "bun": ">=1.4.2"
  },
  "dependencies": {
    "@huggingface/tokenizers": "0.2.0",
    "@modelcontextprotocol/server": "2.0.0",
    "onnxruntime-node": "1.29.0",
    "zod": "4.6.1"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/bun": "1.4.2",
    "prettier": "3.9.6",
    "typescript": "7.0.2"
  }
}
